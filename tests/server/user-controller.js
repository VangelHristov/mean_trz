'use strict';

const userController = require('../../app_api/controllers/user-controller');

const {assert} = require('chai');

suite('user-controller.js');

test('true is true', () => {
	assert.isTrue(true);
});